app = "street-xchange"
primary_region = "lhr"

[build]
  image = "heroku/buildpacks:22" # or :22 (not :24, it doesn’t exist)

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[scale]
  count = 1
